[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.build_meta"

[project]
name = "readflow-video"
version = "0.1.0"
description = "Generate vertical reading videos from prompts."
requires-python = ">=3.12"

[tool.setuptools.packages.find]
where = ["packages"]
